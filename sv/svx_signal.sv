`ifndef SVX_SIGNAL__SV
`define SVX_SIGNAL__SV

// Kept as an installed compatibility include. SVX 1.0 signal access calls the
// native VPI service directly from libsvx and requires no SV dispatcher.

`endif
